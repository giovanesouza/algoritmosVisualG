Algoritmo "tabuada"

Var
   i:inteiro
   v:inteiro


Inicio
   escreva("Digite um valor para a tabuada: ")
   leia(v)

   para i de 1 ate 10 faca

      escreval(v , " x ", i, " = ", v * i)

   fimpara
   
   //v = número digitado | "x" = multiplicação | "=" => = | "i" = repetição
   // primeiro "i" = valor fixo | segundo "i" multiplica o valor digitado "v"

Fimalgoritmo